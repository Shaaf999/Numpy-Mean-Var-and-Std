import numpy as np
n,m= list(map(int,input().split()))
arr=np.array([input().split() for i in range(n)], int)
print(np.mean(arr, axis =1))
print(np.var(arr, axis=0))
o=np.std(arr)
print(o.round(11)) # rounding to 11 decimals as in output
